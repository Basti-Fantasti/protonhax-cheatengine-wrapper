[project]
name = "ce-autostart"
version = "0.1.0"
description = "Automatically start CheatEngine for Steam games running on Proton"
requires-python = ">=3.11"
dependencies = ["requests>=2.31.0"]

[tool.uv]
managed = true
